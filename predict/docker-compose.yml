version: '3.8'

services:
  defect-classifier:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: defect-classifier-service
    ports:
      - "5000:5000"
    environment:
      PYTHONDONTWRITEBYTECODE: 1
      PYTHONUNBUFFERED: 1
